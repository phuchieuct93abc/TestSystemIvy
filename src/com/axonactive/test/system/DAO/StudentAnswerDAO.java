package com.axonactive.test.system.DAO;

import java.util.List;

import javax.persistence.Persistence;

import ch.ivyteam.ivy.environment.Ivy;

import com.axonactive.test.system.converter.StudentAnswerConverter;
import com.axonactive.test.system.model.ExaminationModel;
import com.axonactive.test.system.model.StudentAnswerModel;
import com.axonactive.test.system.service.PersistenceService;

public class StudentAnswerDAO {

	
/*	public static void save(StudentAnswerModel model){
		PersistenceService.getPersistence().persist(StudentAnswerConverter.toEntity(model));
	}
	
	public static void saveList(List<StudentAnswerModel> listModel){
		for (StudentAnswerModel studentAnswerModel : listModel) {
			PersistenceService.getPersistence().persist(StudentAnswerConverter.toEntity(studentAnswerModel));
		}
	}*/
	
}
